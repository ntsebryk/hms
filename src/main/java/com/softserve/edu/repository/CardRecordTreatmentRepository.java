package com.softserve.edu.repository;

import java.util.List;

import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;
import org.springframework.data.jpa.repository.JpaRepository;

import com.softserve.edu.entity.CardRecord;

//public interface CardRecordTreatmentRepository extends JpaRepository<CardRecordTreatment, Integer> {
	
//	List<CardRecordTreatment> findByCardRecord(CardRecord cardRecord); 
	
//	Page<CardRecordTreatment> findByCardRecord(CardRecord cardRecord, Pageable pageable);

//}
